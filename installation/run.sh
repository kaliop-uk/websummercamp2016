#!/usr/bin/env bash

# NB: This file is here to comply with the requirements from Netgen for building the VM
# On the other hand, provisioning the Docker containers makes the size of the VM grow quite a bit, so it was decided
# *not* to do that for the generic VM used by all participants to the summer camp

echo "Nothing to be done... The 'deploy' workshop will not be provisioned as part of the initial build of the VM"
