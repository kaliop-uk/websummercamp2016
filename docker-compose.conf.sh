# A config file defining env vars which can be used in docker-compose.yml
# NB: only lines starting with 'export' will be parsed!
#
# NB: This file is stored in git, and is thus common to the whole project team.

export COMPOSE_PROJECT_NAME=ezdeploy
